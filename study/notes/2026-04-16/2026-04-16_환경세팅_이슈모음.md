# 2026-04-16 | 환경 세팅 & 실행 이슈 TODO

## 해결된 것들

- [x] 루트 폴더 이름 변경 후 venv 경로 깨짐 → venv 재생성으로 해결
- [x] `pip freeze` 안 될 때 → `.venv\Scripts\python.exe -m pip freeze`로 우회
- [x] `apscheduler`, `anthropic`, `beautifulsoup4`, `pytrends`, `openai`, `playwright` 누락 → 개별 설치
- [x] Playwright는 `pip install` 후 `playwright install chromium` 별도 필요
- [x] `.env`에 `DATABASE_URL=DATABASE_URL=...` 중복 → 앞에 하나 제거
- [x] DB 연결 정보 불일치 (user/password가 docker-compose랑 달랐음)
- [x] `idea_notes.py`에 `ForeignKey` import 누락
- [x] Anthropic 크레딧 부족 → OpenAI로 임시 교체 (기존 코드 주석 처리)
- [x] `crawl_trends` 예외가 `with get_session()` 안에서 터져 전체 롤백 → 현재 주석 처리로 회피

## 남은 것들

- [ ] `crawl_trends` 분리 — 별도 `with get_session()` 블록으로 빼서 뉴스 저장과 트랜잭션 분리
- [ ] Google Trends 400 에러 해결 (pytrends 한국 IP 차단 이슈)
- [ ] `news_crawler.py` — `published_at` RFC 형식(`Mon, 14 Apr ...`) → `date` 파싱 처리
- [ ] `policy_detector._check_reanalysis_threshold` — `date.today(timezone.utc)` 오류 수정
- [ ] `_save_extract`, `_save_policy` TODO 남은 구현 완성
- [ ] Anthropic 크레딧 충전 후 다시 Claude로 교체
- [ ] DB에 실제 데이터 들어오는지 end-to-end 확인

## 참고

- docker-compose: `cd mvp2 && docker-compose up -d`
- 서버 실행: `cd mvp2/backend && uvicorn app.main:app --reload`
- DB 접속: `docker exec -it mvp2_postgres psql -U test -d testdb`
